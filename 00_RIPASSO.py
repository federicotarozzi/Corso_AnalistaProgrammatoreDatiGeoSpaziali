# OPERAZIONI 
n1 = 1
n2 = 2
ndiv = n1 / n2 # divisione intera si fa con //
npotenza = n1 ** n2
nmodulo = n1 % n2 #modulo--> riporta il resto della divisione 

# +=, -=, *=, /= --> relatico a un generale --> utile per i contatori. 

# variazioni sulla striga
s2 = "Python per tutti"
print(s2[0:6]) # stampa i caratteri da 0 incluso a 6 ESCLUSO (escludo quindi lo spazio che è il sesto carattere)
print(s2[6:]) # stampo dal sesto carattere in avanti 

len() # è una funzione quindi si usa così 

# questo modo con il .format l'abbiamo visto per gestire meglio le print 
print("Stiamo usando al versione {}" .format(s2)) # valutazione puntata 
